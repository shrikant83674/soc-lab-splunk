# INC-0001 - Possible Brute Force Authentication Attack

## Incident Summary

This incident was generated from a Splunk correlation search that detected multiple failed Windows authentication events. The investigation aimed to identify the source of the failed logons, determine the affected user and host, and verify whether any successful authentication occurred.

## Detection Query

```spl id="j5x9q2"
index=botsv2 sourcetype=wineventlog EventCode=4625
| stats count by src_ip, user
| sort - count
| head 10
```

## Initial Triage

### Query 1

```spl id="q1"
index=botsv2 sourcetype=wineventlog:security EventCode=4625
| stats count by src_ip, user
| sort - count
| head 10
```

**Result:** No events returned.

**Analyst Action:** Validated available sourcetypes in the `botsv2` index to identify the correct Windows event sourcetype.

---

### Query 2

```spl id="q2"
index=botsv2
| stats count by sourcetype
| sort - count
```

**Top Sourcetypes Observed**

* WinRegistry: 371,660
* Perfmon:Process: 307,275
* mysql:server:stats: 108,817
* mysql:transaction:details: 107,135
* collectd: 98,148
* ps: 39,910
* mysql:variables: 27,084
* WinHostMon: 22,525
* Perfmon:LogicalDisk: 20,139
* mysql:status: 18,856
* stream:ip: 16,857
* Perfmon:PhysicalDisk: 16,744
* suricata: 15,407
* wineventlog: 14,492
* stream:tcp: 14,124

**Observation:** Windows event logs are available under the sourcetype `wineventlog`, confirming that authentication events can be investigated.

**Initial Assessment:** The original query failed because the assumed sourcetype was incorrect.

---

### Query 3

```spl id="q3"
index=botsv2 sourcetype=wineventlog
| stats count by EventCode
| sort - count
| head 20
```

**Result:**

* EventCode 4625 (Failed logon): 1,442 events
* EventCode 4624 (Successful logon): 33,627 events

**Assessment:** The dataset contains both failed and successful Windows authentication events.

---

### Query 4

```spl id="q4"
index=botsv2 sourcetype=wineventlog EventCode=4625
| stats count by user
| sort - count
| head 10
```

**Result**

| User          | Failed Logons |
| ------------- | ------------: |
| MERCURY$      |        15,070 |
| al.bungstien  |           745 |
| Administrator |           519 |
| Guest         |            53 |
| mkraeusen     |             4 |
| administrator |             3 |
| hawkeye       |             2 |

**Assessment**

* `MERCURY$` is a machine account and is not the primary investigation target.
* `al.bungstien` is the most suspicious user account with 745 failed logons.
* `Administrator` also shows elevated failed logon activity and should be reviewed separately.

---

### Query 5

```spl id="q5"
index=botsv2 sourcetype=wineventlog EventCode=4625 user="al.bungstien"
| stats count by src_ip
| sort - count
| head 10
```

**Result**

| Source IP | Failed Logons |
| --------- | ------------: |
| 127.0.0.1 |           745 |

**Assessment:** All failed logons originated from localhost (`127.0.0.1`), indicating a local authentication issue rather than an external brute-force attack.

---

### Query 6

```spl id="q6"
index=botsv2 sourcetype=wineventlog EventCode=4625 user="al.bungstien"
| stats count by host
| sort - count
```

**Result**

| Host        | Failed Logons |
| ----------- | ------------: |
| wrk-abungst |           745 |

**Assessment:** All failed logons for `al.bungstien` were generated by the workstation `wrk-abungst`.

---

### Query 7

```spl id="q7"
index=botsv2 sourcetype=wineventlog EventCode=4625 user="al.bungstien"
| timechart span=5m count
```

**Observation:** Failed logons were distributed across an extended period rather than occurring in a single burst.

**Assessment:** The pattern is consistent with a persistent local authentication issue such as a service, application, scheduled task, or script repeatedly attempting logon.

---

### Query 8

```spl id="q8"
index=botsv2 sourcetype=wineventlog EventCode=4624 user="al.bungstien" host="wrk-abungst"
| stats count earliest(_time) as first_success latest(_time) as last_success
```

**Result**

* Successful logons found: **0**

**Assessment:** No successful authentication events were identified for `al.bungstien` on `wrk-abungst`.

## Findings

* Investigated user: **al.bungstien**
* Failed logons observed: **745**
* Source IP: **127.0.0.1 (localhost)**
* Affected host: **wrk-abungst**
* Activity persisted over an extended period.
* Successful logons for the same user and host: **0**

## Impact Assessment

No evidence of account compromise was identified. The activity appears to be a **local authentication anomaly** rather than an external brute-force attack. Business impact is assessed as **Low to Medium** and limited to repeated authentication failures on a single workstation.

## Recommended Actions

1. Review scheduled tasks on `wrk-abungst`.
2. Review Windows services using stored credentials.
3. Check cached credentials and recent password changes for `al.bungstien`.
4. Review application logs on `wrk-abungst` for repeated authentication attempts.
5. Continue monitoring for similar failed logon activity on other hosts.

## MITRE ATT&CK Mapping

* **T1110 - Brute Force**

## Evidence Screenshots

* Query 4 - Targeted users: `../screenshots/inc-0001-query4-target-users.png`
* Query 5 - Source IP analysis: `../screenshots/inc-0001-query5-source-ip.png`
* Query 6 - Host identification: `../screenshots/inc-0001-query6-host.png`
* Query 7 - Time distribution: `../screenshots/inc-0001-query7-timechart.png`
* Query 8 - Successful logon verification: `../screenshots/inc-0001-query8-success-check.png`
* Detection rule results: `../screenshots/inc-0001-detection-results.png`

## Analyst

**Shrikant Raut**
